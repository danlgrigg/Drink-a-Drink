<li>
	{{drink_name}}

	<button class="change-" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if imbibed}}PARTY TIME!{{else}}PARTY'S DONE!{{/if}}
	</button>

	<button class="delete-drink" data-id="{{this.id}}">DELETE!</button>
</li>
