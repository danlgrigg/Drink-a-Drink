<h1>Party!</h1>

<h2>These drinks need to be imbibed!</h2>

<ul>
  {{#each drinks}}
    {{#unless imbibed}}
      {{> drinks/drink-block imbibed=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Empty Drinks</h2>

<ul>
  {{#each drinks}}
    {{#if imbibed}}
      {{> drinks/drink-block imbibed=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Drink</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Drink Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  {{!-- <div class="form-group">
    <label for="imbibed">Imbibed?</label><br>
    <input type="radio" name="imbibed" value="1" checked> Empty!<br>
    <input type="radio" name="imbibed" value="0"> Imbibe!
  </div> --}}

  <button type="submit">Add Drink</button>
</form>
